task fatJar(type: Jar) {
    manifest {
        attributes 'Implementation-Title': 'ChatServer',
                'Main-Class': 'ru.levchugov.chat.server.Runner'
    }
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
}

dependencies {
    compile project(':common')
}
